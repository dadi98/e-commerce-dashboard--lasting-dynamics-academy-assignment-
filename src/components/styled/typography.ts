import Typography from "@mui/material/Typography";
import { styled, } from "@mui/material/styles";

const VisitorsText = styled(Typography)(({theme}) => ({
    height:"79px",
    display:"flex",
    alignItems:"center"
}))

export { VisitorsText }